<?xml version="1.0" encoding="UTF-8"?>
<!--

    Copyright (C) 2022 DANS - Data Archiving and Networked Services (info@dans.knaw.nl)

    Licensed under the Apache License, Version 2.0 (the "License");
    you may not use this file except in compliance with the License.
    You may obtain a copy of the License at

    http://www.apache.org/licenses/LICENSE-2.0

    Unless required by applicable law or agreed to in writing, software
    distributed under the License is distributed on an "AS IS" BASIS,
    WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
    See the License for the specific language governing permissions and
    limitations under the License.

-->
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>nl.datastations</groupId>
        <artifactId>dans-core-systems-parent</artifactId>
        <version>1.0.2</version>
    </parent>

    <groupId>nl.knaw.dans</groupId>
    <artifactId>dd-parent</artifactId>
    <version>3.0.3-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>DANS Data Station Parent POM</name>
    <description>Parent POM for DANS Data Station modules</description>
    <inceptionYear>2022</inceptionYear>
    <scm>
        <connection>scm:git:https://github.com/DANS-KNAW/dd-parent</connection>
        <developerConnection>scm:git:ssh://github.com/DANS-KNAW/dd-parent</developerConnection>
        <tag>HEAD</tag>
        <url>https://github.com/DANS-KNAW/dd-parent</url>
    </scm>

    <properties>
        <dans-provider-name>dans.knaw.nl</dans-provider-name>
        <rpm-scriptlets-dir>${project.build.directory}/rpm-scriptlets</rpm-scriptlets-dir>
        <rpm-replace-configuration>true</rpm-replace-configuration>

        <!-- To be filled in by child project -->
        <rpm-release />
        <main-class />

        <!-- Plugin versions -->
        <maven-assembly-plugin.version>3.7.1</maven-assembly-plugin.version>
        <openapi-generator-maven-plugin.version>7.15.0</openapi-generator-maven-plugin.version>
        <jaxb2-maven-plugin.version>2.5.0</jaxb2-maven-plugin.version>
        <!-- Generates javax.xml classes. When upgrading, we need to go wholesale to jakarta.xml.bind-api -->

        <exec-maven-plugin.version>3.5.1</exec-maven-plugin.version>
        <rpm-maven-plugin.version>2.3.0</rpm-maven-plugin.version>
        <dans-nexus-maven-plugin.version>1.0.0</dans-nexus-maven-plugin.version>
        <dans-build-resources-maven-plugin.version>0.10.0</dans-build-resources-maven-plugin.version>

        <!-- Library versions -->
        <dropwizard-testing.version>3.0.16</dropwizard-testing.version>

        <!-- DANS APIs -->
        <dd-dans-sword2-examples.version>1.2.0</dd-dans-sword2-examples.version>
        <dd-vault-catalog-api.version>1.2.0</dd-vault-catalog-api.version>
        <dd-validate-dans-bag-api.version>1.0.1</dd-validate-dans-bag-api.version>
        <dd-data-vault-api.version>2.1.0</dd-data-vault-api.version>
        <dd-gmh-api.version>0.1.1</dd-gmh-api.version>
        <dd-dataverse-ingest-api.version>0.4.1</dd-dataverse-ingest-api.version>
        <dd-validate-bagpack-api.version>0.2.0</dd-validate-bagpack-api.version>
        <dd-vault-metadata-api.version>1.0.1</dd-vault-metadata-api.version>
        <dd-virus-scan-api.version>0.2.1</dd-virus-scan-api.version>
        <dd-lob-store-api.version>1.3.1</dd-lob-store-api.version>
        <dd-manage-deposit-api.version>1.0.1</dd-manage-deposit-api.version>
        <dd-transfer-to-vault-api.version>0.1.2</dd-transfer-to-vault-api.version>

        <!-- Apache Commons -->
        <commons-lang.version>2.6</commons-lang.version>
        <commons-configuration.version>1.10</commons-configuration.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- DANS APIs not managed by the parent -->
            <dependency>
                <groupId>nl.knaw.dans</groupId>
                <artifactId>dd-dataverse-ingest-api</artifactId>
                <version>${dd-dataverse-ingest-api.version}</version>
            </dependency>
            <dependency>
                <groupId>nl.knaw.dans</groupId>
                <artifactId>dd-dans-sword2-examples</artifactId>
                <version>${dd-dans-sword2-examples.version}</version>
            </dependency>
            <dependency>
                <groupId>nl.knaw.dans</groupId>
                <artifactId>dd-validate-bagpack-api</artifactId>
                <version>${dd-validate-bagpack-api.version}</version>
            </dependency>
            <dependency>
                <groupId>nl.knaw.dans</groupId>
                <artifactId>dd-vault-metadata-api</artifactId>
                <version>${dd-vault-metadata-api.version}</version>
            </dependency>
            <dependency>
                <groupId>nl.knaw.dans</groupId>
                <artifactId>dd-virus-scan-api</artifactId>
                <version>${dd-virus-scan-api.version}</version>
            </dependency>
            <dependency>
                <groupId>nl.knaw.dans</groupId>
                <artifactId>dd-vault-catalog-api</artifactId>
                <version>${dd-vault-catalog-api.version}</version>
            </dependency>
            <dependency>
                <groupId>nl.knaw.dans</groupId>
                <artifactId>dd-lob-store-api</artifactId>
                <version>${dd-lob-store-api.version}</version>
            </dependency>
            <dependency>
                <groupId>nl.knaw.dans</groupId>
                <artifactId>dd-manage-deposit-api</artifactId>
                <version>${dd-manage-deposit-api.version}</version>
            </dependency>
            <dependency>
                <groupId>nl.knaw.dans</groupId>
                <artifactId>dd-transfer-to-vault-api</artifactId>
                <version>${dd-transfer-to-vault-api.version}</version>
            </dependency>

            <!-- Database -->
            <!-- postgresql intentionally overrides the parent's managed version, see plan.md -->
            <dependency>
                <groupId>org.postgresql</groupId>
                <artifactId>postgresql</artifactId>
                <version>${postgresql.version}</version>
            </dependency>

            <!-- Apache Commons -->
            <dependency>
                <groupId>commons-configuration</groupId>
                <artifactId>commons-configuration</artifactId>
                <version>${commons-configuration.version}</version>
            </dependency>
            <dependency>
                <groupId>commons-lang</groupId>
                <artifactId>commons-lang</artifactId>
                <version>${commons-lang.version}</version>
            </dependency>
            <!-- commons-configuration2 intentionally overrides the parent's managed version, see plan.md -->
            <dependency>
                <groupId>org.apache.commons</groupId>
                <artifactId>commons-configuration2</artifactId>
                <version>${commons-configuration2.version}</version>
            </dependency>
            <!-- XML -->
            <!-- Change javax.xml.bind to jakarta.xml.bind-api when we go to jakarta -->
            <!--            <dependency>-->
            <!--                <groupId>jakarta.xml.bind</groupId>-->
            <!--                <artifactId>jakarta.xml.bind-api</artifactId>-->
            <!--                <version>${jakarta.xml.bind-api.version}</version>-->
            <!--            </dependency>-->

        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <!-- maven-jar-plugin: configuration intentionally overrides/extends the parent's
                     (adds classpathPrefix ../lib/ and mainClass for RPM/service packaging
                     layout), see plan.md -->
                <plugin>
                    <artifactId>maven-jar-plugin</artifactId>
                    <version>${maven-jar-plugin.version}</version>
                    <configuration>
                        <archive>
                            <manifest>
                                <addClasspath>true</addClasspath>
                                <classpathPrefix>../lib/</classpathPrefix>
                                <mainClass>${main-class}</mainClass>
                            </manifest>
                            <manifestEntries>
                                <Implementation-Version>${project.version}</Implementation-Version>
                            </manifestEntries>
                        </archive>
                    </configuration>
                </plugin>
                <!-- rpm-maven-plugin -->
                <plugin>
                    <groupId>org.codehaus.mojo</groupId>
                    <artifactId>rpm-maven-plugin</artifactId>
                    <version>${rpm-maven-plugin.version}</version>
                    <configuration>
                        <group>Applications/Archiving</group>
                        <release>${rpm-release}</release>
                        <vendor>${dans-provider-name}</vendor>
                        <packager>${dans-provider-name}</packager>
                        <name>${dans-provider-name}-${project.artifactId}</name>
                        <defaultUsername>root</defaultUsername>
                        <defaultGroupname>root</defaultGroupname>
                        <license>Apache 2.0</license>
                        <targetOS>Linux</targetOS>
                        <defaultFilemode>744</defaultFilemode>
                        <defaultDirmode>755</defaultDirmode>
                        <mappings>
                            <!-- Contents of ${app.home}/lib -->
                            <mapping>
                                <directory>/opt/${dans-provider-name}/${project.artifactId}/lib</directory>
                                <dependency>
                                    <stripVersion>false</stripVersion>
                                </dependency>
                            </mapping>
                            <!-- Read-only contents of ${app.home}/bin  -->
                            <mapping>
                                <directory>/opt/${dans-provider-name}/${project.artifactId}/bin</directory>
                                <sources>
                                    <source>
                                        <location>src/main/assembly/dist/bin</location>
                                        <excludes>
                                            <!-- This is the executable, so exclude here -->
                                            <exclude>${project.artifactId}-${project.version}</exclude>
                                        </excludes>
                                    </source>
                                    <source>
                                        <location>${project.build.directory}/${project.artifactId}-${project.version}.jar</location>
                                    </source>
                                </sources>
                            </mapping>
                            <!-- Executable contents of ${app.home}/bin -->
                            <mapping>
                                <directory>/opt/${dans-provider-name}/${project.artifactId}/bin</directory>
                                <filemode>755</filemode>
                                <sources>
                                    <source>
                                        <location>src/main/assembly/dist/bin/${project.artifactId}</location>
                                    </source>
                                </sources>
                            </mapping>
                            <!-- Contents of cfg dir under /etc/opt ... -->
                            <mapping>
                                <directory>/etc/opt/${dans-provider-name}/${project.artifactId}</directory>
                                <configuration>${rpm-replace-configuration}</configuration>
                                <sources>
                                    <source>
                                        <location>src/main/assembly/dist/cfg</location>
                                    </source>
                                </sources>
                            </mapping>
                            <!-- Contents of ${app.home}/{*-bin/cfg} -->
                            <mapping>
                                <directory>/opt/${dans-provider-name}/${project.artifactId}</directory>
                                <sources>
                                    <source>
                                        <location>src/main/assembly/dist</location>
                                        <excludes>
                                            <exclude>bin/*</exclude>
                                            <exclude>cfg/*</exclude>
                                        </excludes>
                                    </source>
                                </sources>
                            </mapping>
                            <!-- Symlink to executable, to put it on the PATH -->
                            <mapping>
                                <directory>/opt/bin</directory>
                                <sources>
                                    <softlinkSource>
                                        <location>/opt/${dans-provider-name}/${project.artifactId}/bin/${project.artifactId}</location>
                                    </softlinkSource>
                                </sources>
                            </mapping>
                            <!-- Symlink jar to version-less location -->
                            <mapping>
                                <directory>/opt/${dans-provider-name}/${project.artifactId}/bin/</directory>
                                <sources>
                                    <softlinkSource>
                                        <destination>${project.artifactId}.jar</destination>
                                        <location>/opt/${dans-provider-name}/${project.artifactId}/bin/${project.artifactId}-${project.version}.jar</location>
                                    </softlinkSource>
                                </sources>
                            </mapping>
                        </mappings>
                        <defineStatements>
                            <!--
                                To prevent the following problem from occurring:
                                https://github.com/DANS-KNAW/easy-dtap#problem-building-rpm
                                Somehow this macro was not defined on some developers' systems, and they had to work around it by
                                defining it in ~/.rpmmacros. This should no longer be necessary.
                            -->
                            <defineStatement>_tmppath %{_topdir}/BUILD</defineStatement>
                        </defineStatements>
                        <!-- Build-time scripts -->
                        <prepareScriptlet>
                            <scriptFile>${rpm-scriptlets-dir}/build-0-prepare.sh</scriptFile>
                            <fileEncoding>UTF-8</fileEncoding>
                        </prepareScriptlet>
                        <installScriptlet>
                            <scriptFile>${rpm-scriptlets-dir}/build-1-install.sh</scriptFile>
                            <fileEncoding>UTF-8</fileEncoding>
                        </installScriptlet>
                        <cleanScriptlet>
                            <!-- NOTE: I have not been able to establish that this script is executed at all -->
                            <scriptFile>${rpm-scriptlets-dir}/build-2-clean.sh</scriptFile>
                            <fileEncoding>UTF-8</fileEncoding>
                        </cleanScriptlet>

                        <!-- Install/erase-time scripts -->
                        <pretransScriptlet>
                            <scriptFile>${rpm-scriptlets-dir}/0-pre-trans.sh</scriptFile>
                            <fileEncoding>UTF-8</fileEncoding>
                        </pretransScriptlet>
                        <preinstallScriptlet>
                            <scriptFile>${rpm-scriptlets-dir}/1-pre-install.sh</scriptFile>
                            <fileEncoding>UTF-8</fileEncoding>
                        </preinstallScriptlet>
                        <postinstallScriptlet>
                            <scriptFile>${rpm-scriptlets-dir}/2-post-install.sh</scriptFile>
                            <fileEncoding>UTF-8</fileEncoding>
                        </postinstallScriptlet>
                        <preremoveScriptlet>
                            <scriptFile>${rpm-scriptlets-dir}/3-pre-remove.sh</scriptFile>
                            <fileEncoding>UTF-8</fileEncoding>
                        </preremoveScriptlet>
                        <postremoveScriptlet>
                            <scriptFile>${rpm-scriptlets-dir}/4-post-remove.sh</scriptFile>
                            <fileEncoding>UTF-8</fileEncoding>
                        </postremoveScriptlet>
                        <posttransScriptlet>
                            <scriptFile>${rpm-scriptlets-dir}/5-post-trans.sh</scriptFile>
                            <fileEncoding>UTF-8</fileEncoding>
                        </posttransScriptlet>
                        <verifyScriptlet>
                            <!-- NOTE: I have not been able to establish that this script is executed at all -->
                            <scriptFile>${rpm-scriptlets-dir}/6-verify.sh</scriptFile>
                            <fileEncoding>UTF-8</fileEncoding>
                        </verifyScriptlet>
                    </configuration>
                    <dependencies>
                        <dependency>
                            <groupId>org.codehaus.plexus</groupId>
                            <artifactId>plexus-archiver</artifactId>
                            <version>${plexus-archiver.version}</version>
                        </dependency>
                    </dependencies>
                    <executions>
                        <execution>
                            <id>attach-rpm</id>
                            <goals>
                                <goal>attached-rpm</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>
                <!-- maven-assembly-plugin -->
                <plugin>
                    <artifactId>maven-assembly-plugin</artifactId>
                    <version>${maven-assembly-plugin.version}</version>
                    <configuration>
                        <appendAssemblyId>false</appendAssemblyId>
                        <descriptors>
                            <descriptor>src/main/assembly/bin.xml</descriptor>
                        </descriptors>
                    </configuration>
                    <executions>
                        <execution>
                            <id>make-assembly</id>
                            <goals>
                                <goal>single</goal>
                            </goals>
                            <phase>package</phase>
                        </execution>
                    </executions>
                </plugin>
                <plugin>
                    <groupId>nl.knaw.dans</groupId>
                    <artifactId>dans-build-resources-maven-plugin</artifactId>
                    <version>${dans-build-resources-maven-plugin.version}</version>
                    <executions>
                        <execution>
                            <goals>
                                <goal>generate-rpm-scriptlets</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>

                <!-- dans-nexus-maven-plugin - for releasing RPMs -->
                <plugin>
                    <groupId>nl.knaw.dans</groupId>
                    <artifactId>dans-nexus-maven-plugin</artifactId>
                    <version>${dans-nexus-maven-plugin.version}</version>
                    <configuration>
                        <nexusUserName>${env.NEXUS_USER}</nexusUserName>
                        <nexusPassword>${env.NEXUS_PASSWORD}</nexusPassword>
                        <rpmRepositoryUrl>https://nexus.dans.knaw.nl/repository/rpm-releases</rpmRepositoryUrl>
                        <snapshotRpmRepositoryUrl>https://nexus.dans.knaw.nl/repository/rpm-snapshots</snapshotRpmRepositoryUrl>
                    </configuration>
                    <executions>
                        <execution>
                            <!-- Use default phase: deploy -->
                            <goals>
                                <goal>deploy-rpm</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>

                <plugin>
                    <groupId>org.codehaus.mojo</groupId>
                    <artifactId>exec-maven-plugin</artifactId>
                    <version>${exec-maven-plugin.version}</version>
                    <configuration>
                        <mainClass>${main-class}</mainClass>
                    </configuration>
                </plugin>

                <plugin>
                    <groupId>org.codehaus.mojo</groupId>
                    <artifactId>jaxb2-maven-plugin</artifactId>
                    <version>${jaxb2-maven-plugin.version}</version>
                    <configuration>
                        <clearOutputDir>false</clearOutputDir>
                        <noGeneratedHeaderComments>true</noGeneratedHeaderComments>
                        <extension>true</extension>
                    </configuration>
                    <executions>
                        <execution>
                            <goals>
                                <goal>xjc</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>

                <plugin>
                    <groupId>org.openapitools</groupId>
                    <artifactId>openapi-generator-maven-plugin</artifactId>
                    <version>${openapi-generator-maven-plugin.version}</version>
                    <configuration>
                        <generateModelTests>false</generateModelTests>
                        <generateApiTests>false</generateApiTests>
                        <modelNameSuffix>Dto</modelNameSuffix>
                        <configOptions>
                            <sourceFolder>src/gen/java/main</sourceFolder>
                            <dateLibrary>java8</dateLibrary>
                            <delegatePattern>true</delegatePattern>
                            <interfaceOnly>true</interfaceOnly>
                            <returnResponse>true</returnResponse>
                            <useSwaggerAnnotations>false</useSwaggerAnnotations>
                        </configOptions>
                    </configuration>
                </plugin>

            </plugins>
        </pluginManagement>
        <plugins>
            <plugin>
                <groupId>nl.knaw.dans</groupId>
                <artifactId>dans-build-resources-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
            </plugin>
            <plugin>
                <artifactId>maven-javadoc-plugin</artifactId>
                <configuration>
                    <!-- Disable the inherited Javadocs generation; it is basically only useful for libraries -->
                    <skip>true</skip>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>
